# Canonical app version — MUST match the GitHub release tag (without leading v/V).
# Example: GitHub tag V1.05 → VERSION_NAME=1.05
# Required for local builds. CI may override via -PVERSION_NAME from the Git tag.
# Overrides (highest priority first): -PVERSION_NAME=…, .env VERSION_NAME, this file.
VERSION_NAME=1.05
